# PeakCam current engineering roadmap — September 11, 2026

This replaces the operational priorities in the March TASKS list and April launch-gate document. Those documents remain historical records. This is a verification and sequencing update; no release or schema change was made.

## Verified release boundary

- Current production and main: `61c5f264a6e16c827ff2a7a8654dd6d5a07e809d`.
- Vercel production deployment `dpl_8fesa8dgDhqRnC2s6Rt3owAxzKiV` is READY and has both custom-domain aliases.
- PR 69, `25252127b1819202c79f4a5ed1f4a7c49e1e9626`, is open with a READY preview. Its forecast alerts, conditions fix and mobile improvements are not production features yet. Review the required migration 016 and release order.
- Existing product: camera browsing/search, resort pages, maps, snow conditions/forecasts, accounts/recovery, favorites, comparison, dashboard, observed-snow alerts, user reports and Drop In.

## Now

| Priority | Work | Acceptance evidence |
|---|---|---|
| P0 | Diagnose browse/map/snow-report data timeouts seen on current production through Sept 10 | Reproduce the failing read, capture query/connection timing, test the cause-specific fix and observe a defined post-deploy window |
| P0 | Restore Drop In run-ticket configuration | Server-only `DROP_IN_TICKET_KEYS` configured securely; ticket issuance, run validation and leaderboard round trip pass after deploy |
| P1 | Review PR 69 and forecast-alert migration compatibility | Reviewed diff, current schema checked, explicit migration/deployment order, preview and production checks |
| P1 | Complete desktop and physical-mobile journeys | Search → resort → working camera/source and fallback; map; account signup/recovery; favorites return; alert delivery; game controls and run submission |
| P1 | Triage camera gaps | Resort pages without active camera entries have a verified replacement/source link or accurate unavailable state; prioritize Loveland and featured pages |
| P1 | Validate analytics capture | Real event payloads and direct-arrival capture checked; `cam_clicked` kept separate from verified playback; founder/test traffic excluded |
| P1 | Review database security advisories | Check intended public view columns and privileged function execution under actual roles; preserve private-report deny-all access |
| P2 | Restore complete local test prerequisites | GDAL terrain-bake tests pass on prepared runtime; dependency advisories triaged without blind upgrades |

The latest-report view uses `DISTINCT ON (resort_id)` across an append-only table. Separate resort/date indexes exist; no matching `(resort_id, updated_at DESC)` index was observed. Query-plan inspection is a diagnostic next step, not a confirmed timeout root cause or permission to blindly apply an index.

## Verification performed in this task

- Restored local dependencies from committed lockfile with lifecycle scripts disabled; no dependency upgrade or lockfile change.
- 1,411/1,415 tests pass. Four terrain-bake tests fail with `spawnSync gdal_translate ENOENT`.
- TypeScript passes with no diagnostics.
- Live database, deployment metadata and runtime logs inspected through connectors.
- Browser access was refused because an admin-enforced browser policy could not be verified. No visual-production or physical-device pass is claimed.
- Email delivery not tested: designated test inbox is required. Do not trigger bulk alerts to validate one recipient.

## Next

Maintain a twelve-resort quality pilot inside existing coverage. Record owner/provider, official source, allowed embedding, still capture, thumbnail/social/ad scope, attribution, expiry and revocation evidence. A working feed or `cam_feed` label is not a permission grant. Resolve the pending media draft's assumptions before implementing capture/storage.

Measure acquisition and repeat camera use, then test one approved contextual referral and one evidence-backed local commercial pilot. Existing favorites and alerts need validation and adoption work, not duplicate implementations. Further game scope, Pro features and geographic expansion depend on retained-user and operating evidence.

Private business evidence, actual adoption counts, invoice reconciliation, all 36 legacy-roadmap dispositions and pilot inquiry drafts are saved locally in `~/Code/peakcam-business/status-2026-09-11/`. They are deliberately outside this public repository. No outreach was sent.
