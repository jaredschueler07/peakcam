# Auth completion contract and review scope

Request: continue the remaining account/email recommendations from the prior PeakCam audit. Base `a74cbbf`; isolated branch `review/auth-email-setup`, target main after validation. Unrelated snowing-now files remain in their own checkout.

The account lifecycle must be reachable consistently: authenticated users can see their email, set a password and sign out locally or globally; logged-out visitors to `/account` are redirected through sign-in with a safe destination. Confirmation resend uses the resend endpoint, never repeats signup, and enforces a UI cooldown in addition to provider limits. Backend diagnostics are not shown verbatim. Sign-in codes are passed only in the verification request body, never URLs, analytics or logs, and establish the normal SSR cookie session. Invalid/reused codes receive an actionable error. Existing magic links stay usable with the code feature disabled.

Owner boundaries: auth form, error copy, account route, account navigation, shared password component, email templates. No account deletion, user migration, RLS/schema changes, SMTP secrets in code, paid services, or weakening of session checks. Supabase remains the password/session/OTP authority. The code UI flag must not be enabled until matching email templates are installed. `supabase/email-templates/README.md` records exact prerequisites, compatibility limits and rollback. Browser checks run against a local-only synthetic Auth server, which cannot send mail or mutate real users.

External acceptance still requires dashboard access: inspect/back up current SMTP/templates/URL/rate/session/password settings, verify a scoped auth sender and DNS alignment, configure branded templates, and validate authorized test messages in real inboxes. Production credentials must never be logged, committed, or included in the review bundle. The live sender is not considered fixed by template generation or mock tests.

Local validation: TypeScript, changed-file ESLint, auth error/safe redirect unit tests, Chromium/WebKit auth lifecycle checks, production build, and source-aware autoreview. Runtime checks are not claimed to be source-blind or physical-device testing. The reviewer should judge the auth lifecycle and prepared deployment contract, including existing sibling shared forms; the existing email delivery configuration remains an external pending item.

Validation result: 1,123 unit tests passed; 10/10 synthetic Auth lifecycle browser checks passed across Chromium and WebKit; 4/4 existing auth-modal/recovery checks passed against the production build with the code flag off. A further production-build probe confirmed optional code entry and guarded account access. TypeScript, changed-file ESLint and the production build passed. The six template payloads match their HTML sources; the code email rendered at 390px without overflow, links or remote images. Final autoreview command: `/Users/maestro_admin/.agents/skills/autoreview/scripts/autoreview --mode local --prompt-file docs/reviews/auth-implementation-contract.md --output /tmp/peakcam-auth-review.md --json-output /tmp/peakcam-auth-review.json`. Exit 0 with no actionable P0 findings; TruffleHog clean. No lower-severity completeness claim is implied.
